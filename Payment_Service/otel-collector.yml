receivers:
  otlp:
    protocols:
      http:

exporters:
  otlp:
    endpoint: jaeger:4318
    tls:
      insecure: true

service:
  pipelines:
    traces:
      receivers: [otlp]
      exporters: [otlp]
